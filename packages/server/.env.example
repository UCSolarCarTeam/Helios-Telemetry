# Local environment variables for the server package
# Copy to .env.local (or set in your environment) and fill values as needed.

# Loki (Grafana Loki) - if LOKI_URL is set the logger will send logs to Loki
# Example: https://loki.example.com or http://loki:3100
LOKI_URL=

# Authentication options (choose one):
# - Provide a base64 encoded user:pass
LOKI_BASIC_AUTH=
# - Or provide username/password and the code will encode them for you
LOKI_USERNAME=
LOKI_PASSWORD=

# Optional static labels to attach to every Loki stream (comma-separated key=value)
# Example: LOKI_LABELS=app=helios,env=development,team=ucsolarcar
LOKI_LABELS=

# Local logging settings
LOG_DIR=logs
LOG_FILE=app.log
LOG_LEVEL=info
# When true, include full error stack traces for ERROR-level logs in console output
LOG_ERROR_STACK=false

# Node environment settings
NODE_ENV=development
ENV=development
